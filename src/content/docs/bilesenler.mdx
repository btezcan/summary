---
title: Bileşen Örnekleri
description: Yazarlar için iç sayfa — tüm bileşenlerin örnek kullanımı.
pagefind: false
head:
  - tag: meta
    attrs:
      name: robots
      content: noindex
---

import { Sample, Output, Predict, Quiz, Mistake, Note, Compare, ProgressChecklist } from '~/components';

Bu sayfa kenar çubuğunda yer almaz. Bileşenlerin her iki temada ve telefon
genişliğinde nasıl göründüğünü kontrol etmek için kullanılır. Buradaki tüm kod
ve çıktılar `samples/bilesenler/` altındaki doğrulanmış örneklerden gelir.

## Sample

<Sample id="bilesenler/merhaba" />

Yalnızca bir bölge, satır vurgusuyla:

<Sample id="bilesenler/merhaba" region="toplam" mark="3" />

Klavyeden girdi alan örnek:

<Sample id="bilesenler/girdi" />

Derleyici uyarısı veren örnek:

<Sample id="bilesenler/uyari" />

Çok dosyalı proje, tek dosya gösterimi:

<Sample id="bilesenler/proje" file="Student.cs" output={false} />
<Sample id="bilesenler/proje" />

## Output (tek başına)

<Output text="Merhaba Dünya!" />

## Mistake

<Mistake title="Atanmamış değişken" wrong="bilesenler/derleme-hatasi" right="bilesenler/merhaba">
  Yerel bir değişken kullanılmadan önce mutlaka bir değer almalıdır (definite assignment).
</Mistake>

<Mistake title="Ondalıklı metni int'e çevirmek" wrong="bilesenler/calisma-hatasi" right="bilesenler/merhaba" rightRegion="toplam" />

## Predict

<Predict id="bilesenler/kultur">
  `tr-TR` kültüründe küçük `i` harfinin büyüğü `İ` olur. Komut ve dosya adları
  gibi kültürden bağımsız metinlerde `ToUpperInvariant` kullanın.
</Predict>

## Quiz

<Quiz questions={[
  {
    question: '`7 / 2` ifadesinin değeri nedir?',
    options: ['`3.5`', '`3`', '`4`', 'Derleme hatası'],
    answer: 1,
    explanation: 'İki `int` bölününce sonuç da `int` olur; ondalık kısım **atılır** (yuvarlanmaz).',
  },
  {
    question: 'Bu program ne yazdırır?',
    sample: 'bilesenler/yeni-surum',
    options: ['`123`', '`6`', 'Derleme hatası'],
    answer: 1,
    explanation: '`params List<int>` C# 13 ile gelen bir özelliktir; sayılar listeye toplanır ve `Sum` 6 döndürür.',
  },
]} />

## Note

<Note type="tip">Önce Özet bölümünü okuyun.</Note>
<Note type="warning">`double` para hesabı için uygun değildir.</Note>
<Note type="version" version="C# 13">`params` artık koleksiyonlarla da kullanılabilir.</Note>
<Note type="history">`ArrayList` eski kodlarda görülür; bugün `List<T>` kullanın.</Note>

## Compare

<Compare
  caption="Tamsayı türleri (kısaltılmış)"
  headers={['C#', '.NET', 'Boyut']}
  rows={[
    ['`int`', '`Int32`', '4 bayt'],
    ['`long`', '`Int64`', '8 bayt'],
  ]}
/>

<Compare items={[
  { title: 'Tek dosya', id: 'bilesenler/merhaba', region: 'toplam' },
  { title: 'Proje', id: 'bilesenler/proje' },
]} />

## İlerleme listesi

<ProgressChecklist />
Bağlantı testi: [Türler](/turler/) ve [çapa](/turler/#ozet).
